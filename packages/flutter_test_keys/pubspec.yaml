name: flutter_test_keys
description: JS bridge + key overlay for Flutter web — lets Playwright drive your app via window.__flutter_* globals.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
